<% provide(:title, 'Search school')%>

<h1>Search school</h1>
<div class="row">
 <div class="col-md-6 col-md-offset-3">
<%= form_for(@schoollist) do |f| %>
  <% if @schoollist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schoollist.errors.count, "error") %> prohibited this schoollist from being saved:</h2>

      <ul>
      <% @schoollist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
</div>
